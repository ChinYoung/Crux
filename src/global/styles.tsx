import { StyleSheet } from 'react-native';

export const GlobalStyles = StyleSheet.create({
  debug: {
    borderColor: 'blue',
    borderStyle: 'solid',
    borderWidth: 1,
  },
  description: {
    fontSize: 12,
    opacity: 0.4,
  },
});
